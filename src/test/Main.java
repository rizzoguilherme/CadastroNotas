package test;

import classes.Menu;

public class Main {
    static void main() {
        // Média >= 7 - aprovado | Média >= 5 e < 7 - recuperação | Média < 5 - reprovado

        // Criar um MENU
        Menu menu = new Menu();
        menu.mostrarMenu();
    }
}
